# Configure only in the private collector runtime, never commit real values.
# Choose an embedding model and matching supported dimensions before enabling indexing.
OPENAI_API_KEY=
BIOVERACITY_EMBEDDING_MODEL=
BIOVERACITY_EMBEDDING_DIMENSIONS=
